#ifndef HEADER_H
#define HEADER_H

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int binarySearch(int key, int mas[], int SIZE);
int wider(int i, int key, int mas[],int SIZE);

#endif // HEADER_H
